[build]
  base = "client"
  publish = "build"
  functions = "../netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# Make sure the catch-all redirect comes last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200