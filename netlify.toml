[build]
  base = "client"
  publish = "build"
  functions = "../netlify/functions"  # Adjust the path if needed

[dev]
  functions = "../netlify/functions"
  
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
